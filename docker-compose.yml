services:
  srt-splitter:
    build: .
    container_name: srt-splitter
    ports:
      - "9000:9000/udp"
    environment:
      - YT_KEY_EN=${YT_KEY_EN}
      - YT_KEY_DE=${YT_KEY_DE}
      - SRT_INPUT_PASSPHRASE=${SRT_INPUT_PASSPHRASE}
    restart: unless-stopped
