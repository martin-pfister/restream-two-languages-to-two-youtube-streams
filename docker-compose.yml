services:
  srt-splitter:
    build: .
    container_name: srt-splitter
    ports:
      - "9000:9000/udp"
    environment:
      - YT_KEY_EN=${YT_KEY_EN}
      - YT_KEY_DE=${YT_KEY_DE}
    restart: unless-stopped
